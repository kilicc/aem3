// This is your Prisma schema file for type generation only
// DO NOT use Prisma Client for database operations
// Use Supabase JS client instead

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
